<?xml version="1.0" encoding="utf-8"?>
<!-- Production default: HTTPS only. Cleartext HTTP is scoped to the known
     development backends (artisan serve on the LAN / emulator loopback) so dev
     builds keep working while a release build can never silently talk HTTP to
     an arbitrary host. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">vsbackend.test</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">192.168.8.20</domain>
    </domain-config>
</network-security-config>
